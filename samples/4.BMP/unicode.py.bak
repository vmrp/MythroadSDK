t = []
t.append("16bit位图处理")
t.append("256色位图处理")
t.append("16色位图处理")
t.append("16色位图处理")
t.append("16色位图处理")
t.append("退出演示")

for s in t:
  uni = s.decode("cp936")
  u = ''
  for c in uni:
    c = ord(c)
    u = u + '\\' + hex(c / 256)[1:] + '\\' + hex(c % 256)[1:]
  print s
  print u
